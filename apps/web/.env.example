# URL of the swarmboard API.
# Leave empty for local dev (Vite proxies /api, /webhooks and /socket.io to localhost:3001).
# In production, set this to the public URL of your deployed API service, e.g.
#   VITE_API_URL="https://swarmboard-api.up.railway.app"
VITE_API_URL=""
